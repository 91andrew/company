#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdbool.h>
#define MAX 90



 struct database{
	int emp[20];
	int man[20];
}company;


int main(){
	
		char c[30];
	int i =0,k=0;
	
	for(i=0,k=0; (c[i]=getchar())!=';';i++){
		if(c[i]>=48 && c[i]<=57){
			if(c[i-6]=='e')company.emp[k]= c[i]-48;
			if(c[i-6]=='r')company.man[k]= c[i]-48;
			
		}
		
		else if(c[i]=='}' ){
			printf("%d ", company.emp[k]);
			printf("%d ", company.man[k]);
			k++;
		}
		
		if(c[i]=='m' )i=0;
		}

	
return 0;
}
